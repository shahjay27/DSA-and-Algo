
 #include<iotream>

 using namespace std;

 int SortedRotatedArraySearch(int arr[], int n, int searchValue)
 {
     int mid=(low+high)/2;

     if(arr[mid]==searchValue)
        return mid;
     else if(arr[low]<=arr[mid])
     {
         if()
            return SortedRotatedArraySearch(arr, low, mid-1, searchValue)
     }

 }
